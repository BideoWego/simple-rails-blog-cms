<h1>Admin</h1>
<div class="logout">
		<%= link_to("Logout", :controller => 'access', :action => 'logout') %>
</div>
<div class="menu">
	<h2>Resources</h2>
	<ul>
		<%
		links = [
			link_to("Authors", :controller => 'author', :action => 'index'),
			link_to("Posts", :controller => 'post', :action => 'index'),
			link_to("Comments", :controller => 'comment', :action => 'index')
		].each do |i|
		%>

		<li>
			<%= i %>
		</li>
		
		<% end %>
	</ul>
</div>